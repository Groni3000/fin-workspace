[package]
name = "instrid"
version = "0.1.0"
edition = "2024"
description = "Financial instrument identification types"
license = "Apache-2.0"

[dependencies]

[build-dependencies]
csv = { version = "1" }

[features]
default = []
mic-full = []
