[package]
name = "futchain"
version = "0.1.0"
edition = "2024"
description = "Futures contract chain navigation and end-of-trading date rules"
license = "MIT OR Apache-2.0"

[dependencies]
chrono = "0.4.44"
instrid = { path = "../instrid" }
